import { BatteryInfoContainer } from './battery-info-container.model';

describe('BatteryInfoContainer', () => {
  it('should create an instance', () => {
    expect(new BatteryInfoContainer()).toBeTruthy();
  });
});
