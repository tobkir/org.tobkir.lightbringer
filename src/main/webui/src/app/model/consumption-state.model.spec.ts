import { ConsumptionState } from './consumption-state.model';

describe('ConsumptionState', () => {
  it('should create an instance', () => {
    expect(new ConsumptionState()).toBeTruthy();
  });
});
