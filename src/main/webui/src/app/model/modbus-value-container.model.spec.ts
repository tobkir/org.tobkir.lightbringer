import { ModbusValueContainer } from './modbus-value-container.model';

describe('ModbusValueContainer', () => {
  it('should create an instance', () => {
    expect(new ModbusValueContainer()).toBeTruthy();
  });
});
