import { GeneralInfoContainer } from './general-info-container.model';

describe('GeneralInfoContainer', () => {
  it('should create an instance', () => {
    expect(new GeneralInfoContainer()).toBeTruthy();
  });
});
